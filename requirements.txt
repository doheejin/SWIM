# Core dependencies for the SFT / GRPO / evaluation pipeline
# (generation/, rl/, aes/, utils/). See prompting/requirements.txt for the
# separate Claude/GPT prompting-baseline pipeline.

torch>=2.4
transformers>=4.51
datasets>=2.16
numpy
pandas
scipy
scikit-learn
tqdm
sentencepiece   # required by the T5-based AES tokenizer

# Fine-tuning / RL
trl>=1.1.0
peft>=0.13.0,<0.19.0   # 0.19.0 references torch.float8_e8m0fnu, needs torch>=2.7
accelerate>=1.13.0
bitsandbytes>=0.49.2   # 4-bit QLoRA

# Optional: faster GRPO rollout generation (rl/grpo_train.py --use_vllm)
vllm>=0.8

# Experiment tracking
wandb
